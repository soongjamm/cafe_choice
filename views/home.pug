extends layouts/main
include mixins/cafeBlock


block content
    each item in cafes
        +cafeBlock({
            id : item.id,
            name : item.name,
            location : item.location,
            imgFile : item.imgFile
        })
    