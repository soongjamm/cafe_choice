extends layouts/main
include mixins/cafeBlock

block content
    .search__header
        each item in cafes
            +cafeBlock({
                id : item.id,
               name : item.name,
               location : item.location,
               imageUrl : item.imageUrl
            })
        h2 #{searchingBy}(으)로 검색한 결과 